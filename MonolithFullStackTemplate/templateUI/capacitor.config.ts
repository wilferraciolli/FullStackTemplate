import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.template.ui',
  appName: 'template-ui',
  webDir: 'dist/template-ui'
};

export default config;
